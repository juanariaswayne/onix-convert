var _ = require('lodash');
var utils = require('../utils');

var Price = require('./price');

module.exports = {
    tag: 'Supplier',
    fields: {
        name: {
            tag: 'SupplierName'
        }
    }
};
